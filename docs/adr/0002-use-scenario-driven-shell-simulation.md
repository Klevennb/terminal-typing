# Use scenario-driven shell simulation

The app will model only the commands, keyboard actions, and state transitions required by authored challenge scenarios rather than emulate complete shells. This keeps practice deterministic, safe, browser-based, and testable while allowing unsupported input to be explained honestly; broader shell behavior can be added scenario by scenario as the curriculum grows.
