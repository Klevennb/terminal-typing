# Do not remap browser-incompatible bindings

Because some authentic shell bindings conflict with shortcuts owned by the browser or operating system, scored challenges will include only bindings that the current environment can capture safely and reliably. Incompatible bindings will be identified and explained rather than silently remapped, preserving truthful instruction without asking learners to weaken browser or operating-system protections.
